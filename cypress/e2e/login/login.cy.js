/// <reference types="cypress" />

context('Acesso a página de login', () =>{
    beforeEach(() => {
        cy.visit('https://www.saucedemo.com/')
    });

    it('Login válido', () => {
        
    });
})